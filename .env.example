# Neon Postgres (connection string do console Neon — use a URL com pooler se for Netlify)
DATABASE_URL=postgresql://USER:SENHA@ep-xxxx.region.aws.neon.tech/neondb?sslmode=require

# NextAuth — gere com: openssl rand -base64 32
NEXTAUTH_SECRET=troque_por_um_segredo_longo
NEXTAUTH_URL=http://localhost:3000
